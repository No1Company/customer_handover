apiVersion: v1
kind: Service
metadata:
  labels:
    app: rostbot
  name: directline-backend-service
spec:
  ports:
  - name: web
    port: 80
    protocol: TCP
    targetPort: 4000
  selector:
    app: rostbot
    service: backend
  type: ClusterIP
