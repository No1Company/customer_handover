apiVersion: v1
kind: Service
metadata:
  labels:
    app: rostbot
  name: rostbot-frontend-service
spec:
  ports:
  - name: web
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: rostbot
    service: frontend
  type: ClusterIP
